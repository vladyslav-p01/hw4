<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title></title>
</head>
<body>
    <form method="get" action="index.php">
        <fieldset>
            <legend align="center">Enter vendor name</legend>
            <label for="vendor">vendor</label>
            <input type="text" name="nameVendor" id="vendor" required value="{{ vendor.name }}">
            <input type="hidden" name="idVendorToDb" value="{{ vendor.id }}">
            <input type="submit" value="Send">
        </fieldset>
        <input type="hidden" name="controller" value="vendor">
        <input type="hidden" name="action" value="{{ action }}">
    </form>
</body>
</html>